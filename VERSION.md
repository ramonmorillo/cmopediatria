# Versión

**1.1.0** — Seguimiento longitudinal y consolidación local segura de registros.

Ver `CHANGELOG.md` para el detalle de cambios. La versión de la aplicación se expone también en
`src/clinical.js` (`export const VERSION`) y se incluye en el JSON exportado
(`exportCase`/`importCase`, esquema `siaf-cmo-pediatria-v1`).
